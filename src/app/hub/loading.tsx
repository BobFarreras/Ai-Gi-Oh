// src/app/hub/loading.tsx - Fallback de carga para la entrada principal del Hub mientras se hidratan datos iniciales.
import { HubSectionLoading } from "@/components/hub/sections/HubSectionLoading";

export default function HubLoadingPage() {
  return <HubSectionLoading label="Sala de Control" />;
}
