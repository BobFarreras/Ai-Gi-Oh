// src/services/story/duel-flow/story-defeat-penalty.ts - Penalización única de Nexus al perder/abandonar un duelo Story (server + aviso UI comparten este valor).

/** Nexus que se pierde al ser derrotado o abandonar un duelo Story. */
export const STORY_DEFEAT_NEXUS_PENALTY = 50;
